# TheGamesDB API key (primary game-metadata source).
# Keyed, hard ~1000 requests/month budget — the metadata service caches
# aggressively and falls back to libretro so this key is hit at most once per
# game per TTL window. Rotate immediately if it ever leaks.
TGDB_API_KEY=
